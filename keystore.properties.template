# 复制为 keystore.properties 并填入真实值；keystore.properties 不要提交到 git
# 生成 keystore（本地执行一次，妥善保管 release.jks 和密码，丢失后无法再更新已上架应用）：
# keytool -genkey -v -keystore release.jks -keyalg RSA -keysize 2048 -validity 36500 \
#         -alias touch_blocker -storepass <STORE_PWD> -keypass <KEY_PWD> \
#         -dname "CN=Touch Blocker, OU=Dev, O=YourOrg, L=City, S=State, C=CN"

storeFile=../release.jks
storePassword=YOUR_STORE_PASSWORD
keyAlias=touch_blocker
keyPassword=YOUR_KEY_PASSWORD
